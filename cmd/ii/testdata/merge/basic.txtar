# merge command test

cd repo
exec jj git init
exec jj bookmark create main -r @

# missing argument
! exec $II merge
stderr 'accepts 1 arg'

# invalid change-id
! exec $II merge abc123
stderr 'rebase onto main'
