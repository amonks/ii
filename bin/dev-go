#!/usr/bin/env fish

# we run esbuild when handling http requests, which requires places/ts/node_modules
cd places/ts
yarn
cd -

begin
	fd -e go -e gohtml
	echo places/ts/index.ts
end | entr -r go run .

