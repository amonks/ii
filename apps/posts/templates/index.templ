package templates

import (
	"monks.co/apps/posts/model"
)

templ Index(posts *model.Posts) {
	@page("Posts") {
		<ul>
			for _, post := range posts.List {
				if !post.IsDraft {
					<li>
						<a href={ templ.SafeURL(post.Slug) }>
							<em>
								{ post.Date }
							</em>
							{ post.Title }
						</a>
					</li>
				}
			}
		</ul>
	}
}
