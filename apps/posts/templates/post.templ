package templates

import "monks.co/apps/posts/model"

templ Post(post *model.Post) {
	<!DOCTYPE html>
	<html>
		<head>
			<title>{ post.Title }</title>
		</head>
		<body>
			<h1>{ post.Title }</h1>
			<h2>{ post.Date }</h2>
			{! post}
		</body>
	</html>
}
