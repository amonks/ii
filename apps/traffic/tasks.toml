[[task]]
  id = "dev"
  type = "long"
  watch = [
    "**/*.go", "*.go",
    "**/*.gohtml", "*.gohtml",
  ]
  cmd = "go run . -port=3008"
  dependencies = ["build-css"]

[[task]]
  id = "build"
  type = "short"
  cmd = "cd $MONKS_ROOT && go build -o ./bin/ ./apps/traffic"
  dependencies = ["build-css"]

[[task]]
  id = "start"
  type = "long"
  cmd = "$MONKS_ROOT/bin/traffic -port=3008"

[[task]]
  id = "build-css"
  type = "short"
  dependencies = ["install-css"]
  watch = [
    "css/*.css",
    "templates/*.gohtml"
  ]
  cmd = """
    cd css
    npx tailwindcss \
      -i ./index.css \
      -o ../static/index.css
  """

[[task]]
  id = "install-css"
  type = "short"
  cmd = "cd css && npm install"

