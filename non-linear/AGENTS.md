We're making a library in ./creamery for working on ice cream formulation problems. example.py demonstrates using the library to create a recipe for a given formulation using ingredients on-hand, and then it demonstrates deriving a possible recipe from a Nutrition Facts panel.

`uvx ty check` to run the typechecker.
`uv run python example.py` to run the example.
