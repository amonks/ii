# setup repo
mkdir repo
cd repo
exec jj git init

# list does not prompt when store missing
exec $II list
stdout 'No todos found\.'

# create without prompt in non-tty
stdin accept.txt
exec $II create --title 'First todo'
stdout 'Created todo'

-- repo/accept.txt --
y
