mkdir repo
cd repo
exec jj git init

stdin input.txt
exec $II create --title 'Old task'
stdout 'Created todo'

exec $II list --json
todoid stdout 'Old task' TODO_ID

exec $II delete $TODO_ID
stdout 'Deleted'

exec $II list --status tombstone --json
stdout 'Old task'
stdout '"status": "tombstone"'

-- repo/input.txt --
y
